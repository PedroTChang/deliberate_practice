// setTimeout(function () {
//   console.log("First task done!");
//   setTimeout(function () {
//     console.log("Second task done!");
//     setTimeout(function () {
//       console.log("Third task done!");
//     }, 2000);
//   }, 2000);
// }, 2000);

// Solve the following problems first in Ruby, then convert the solution to JavaScript.
// 1) Write a function that takes in an array of numbers and returns its sum.
function sumArray(numbers) {
  forEach.number(i = 0; i < array.length; i++)
  number += number[i];
}
console.log(sumArray([1, 2, 3, 4, 5]))
// 2) Write a function that takes in an array of strings and returns the smallest string.
// 3) Write a function that takes in an array of numbers and returns a new array with the numbers in reverse order.
// 4) Write a function that takes in an array of words and returns the number of words that begin with the letter “a”.
// function words(array) {
//   for (let index = 0; index < array.length; index++) {
//     if (array[index][0] == "a") return array[index];
//   }
// }
// console.log(words(["apple", "letter", "dog", "wow"]));

// 5) Write a function that takes in an array of strings and joins them together to make a single string separated by commas.
function words(words) {
  for (let index = 0; index < words.length; inde)
}
// 6) Write a function that takes in an array of numbers and returns the product of all the numbers (each number multiplied by each other).
// 7) Write a function that takes in an array of numbers and returns the two smallest numbers.
// 8) Write a function that takes in an array of numbers and returns a count of how many zeros are in the array.
// 9) Write a function that takes in an array of numbers and returns true if all the numbers are bigger than 10, otherwise returns false.
// 10) Write a function that takes in an array of words and returns the number of times the letter “a” appeared in total.

// //* 1.)
// function mySum(array1) {
//   let initialValue = 1;
//   let sumWithInitial = array1.reduce((previousValue, currentValue) => previousValue + currentValue, initialValue);
//   return sumWithInitial;
// }
// console.log(mySum([1, 2, 3, 4, 5, 6, 7]));

// //* 2.)
// function shortestString(array) {
//   var shortest = array.reduce(function (a, b) {
//     return a.length <= b.length ? a : b;
//   });
//   return shortest;
// }

// console.log(shortestString(["sum", "fire", "pencil", "door", "lemonade"]));

// //* 3.)
// function reverseOrder(array) {
//   let newArray = [];
//   for (let index = array.length - 1; index >= 0; index--) {
//     newArray.push(array[index]);
//   }
//   return newArray;
// }
// console.log(reverseOrder([1, 2, 3, 4, 5, 6, 7]));

// //* 4.)
